{
  "version": "0.0.1",
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "build": "pnpm turbo build",
    "dev": "pnpm turbo dev",
    "lint-fix": "pnpm turbo lint-fix",
    "design-system": "cd packages/ui && pnpm storybook",
    "c": "claude --model sonnet --strict-mcp-config --mcp-config ./.mcp.json --ide"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@anthropic-ai/claude-code": "^1.0.89",
    "next": "15.5.0",
    "turbo": "^2.5.6"
  }
}
